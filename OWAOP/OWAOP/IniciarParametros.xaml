<Page x:Class="OWAOP.IniciarParametros"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
	Title="Iniciar los parametros" Loaded="CargarControles">

    <Grid Margin="10,0,10,10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <ComboBox x:Name="cmbProyecto" Grid.Column="0" Grid.Row="0"></ComboBox>
        <TextBox x:Name="txtAlpha" Grid.Column="0" Grid.Row="1">0,5</TextBox>
        <Button x:Name="btnSiguiente" Grid.Column="0" Grid.Row="2" Margin="0,10,0,0" Width="125"
        Height="25" HorizontalAlignment="Right" Click="btnSiguiente_Click">Siguiente</Button>
    </Grid>
    
</Page>
