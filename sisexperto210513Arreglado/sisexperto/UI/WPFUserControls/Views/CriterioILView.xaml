<UserControl x:Class="sisexperto.UI.WPFUserControls.Views.CriterioILView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:sisexperto.UI.WPFUserControls.Custom" mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="{Binding Nombre}" HorizontalAlignment="Center"/>

        <Slider Grid.Row="1"
                Padding="10"
                Minimum="0" 
                Maximum="{Binding MaximaEtiqueta}" 
                Orientation="Vertical"
                HorizontalAlignment="Center"
                IsSnapToTickEnabled="True" 
                TickFrequency="1" 
                TickPlacement="TopLeft" Value="{Binding EtiquetaSeleccionada, Mode=TwoWay}">
        </Slider>
    </Grid>
</UserControl>
